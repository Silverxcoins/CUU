#ifndef ERRORSWINDOW_H
#define ERRORSWINDOW_H

#include <QDialog>

class ErrorsWindow : public QDialog {
    Q_OBJECT

public:
    explicit ErrorsWindow(QWidget *parent = 0);

    ~ErrorsWindow();

private slots:

private:

};
#endif // ERRORSWINDOW_H
